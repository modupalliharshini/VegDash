// useBodyColor is a no-op in React Native.
// In a web app this would set document.body.style.backgroundColor,
// but React Native doesn't have a DOM.
export const useBodyColor = (_color: string) => {
  // no-op
};
